import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cgtech.silvateam',
  appName: 'Silva Team',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
